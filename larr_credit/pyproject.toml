[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "larr-credit"
version = "0.1.0"
description = "Model-independent LARR phoneme reward and credit assignment"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
